[package]
name = "builder"
edition.workspace = true
version.workspace = true
repository.workspace = true
authors.workspace = true
readme.workspace = true

[features]
enum_ord = ["dep:enum-map"]
editor = ["enum_ord"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
itertools = "0.10.5"
serde = { version = "1.0.164", features = ["derive"] }
ron = "0.8.0"
serde_test = "1.0.164"
log = "0.4.19"
im = "15.1.0"
enum-map = {version = "2.5.0", features = ["serde"], optional = true}

[dev-dependencies]
builder = { path = ".", features = ["enum_ord"] }
